---
id: cmpr12lsa60ac902be46d5e9b
title: 需求详情内的产物内容优化
doc_type: requirement
status: delivered
created: 2026-05-29T14:39:16.426Z
analysis_input_hash: ed03ccefe3e28bac30f2f72abf9137f1f25897eb75d400b229c1edd243819c80
analysis_applied_at: 2026-05-30T06:55:15.122Z
---

> ⚠️ Requirement status canonical 在本 md，Console 仅投影展示。

## 需求描述

我看到产出的 需求文档 没有按模板模块输出（是规则没有限制使用模板还是因为其他问题？），我理解现在的 需求文档内容有一些是用来console ui的解析对应的，所以你重新 把当前“Ai解析”步骤需求文档的产出的内容和模板做个二次结合（参考当前已有的需求文档“/home/sue/dev/SU-CCB/docs/02_需求设计/对接ccb面板的tip动态编写-91b0d7-需求.md” 和 “/home/sue/dev/SU-CCB/su-ccb-claude-plugin/templates/docs/02_需求设计/_模板_需求.md ”）  确保 Ai解析 产出的 需求文档产物 按照模板的模块来产出。其他文档也要思考一下
## 原话（verbatim）

1. 我看到产出的 需求文档 没有按模板模块输出（是规则没有限制使用模板还是因为其他问题？），我理解现在的 需求文档内容有一些是用来console ui的解析对应的，所以你重新 把当前“Ai解析”步骤需求文档的产出的内容和模板做个二次结合（参考当前已有的需求文档“/home/sue/dev/SU-CCB/docs/02_需求设计/对接ccb面板的tip动态编写-91b0d7-需求.md” 和 “/home/sue/dev/SU-CCB/su-ccb-claude-plugin/templates/docs/02_需求设计/_模板_需求.md ”）  确保 Ai解析 产出的 需求文档产物 按照模板的模块来产出。其他文档也要思考一下

## Claude 解读

> 本节经 requirement_analysis 节点产出：plugin lib + Console（indexer / parser / API / Web）源码核验 + slot1_codex 协商（job_2deef2dc7289）+ 用户 2026-05-30 拍板归一方向与范围。

### 功能本质

让 AI 解析（`requirement_analysis` 节点）产出的需求文档**按 `_模板_需求.md` 的模块结构组织**，取代当前固定的「需求描述 + 原话 + Claude 解读 + 歧义点 + 保真差异」5 段式，使人读文档与团队需求写作模板一致。

### 根因：代码写死，不是「规则没限制使用模板」

直接回答用户的疑问——是后者：

- AI 解析落盘走 `lib/requirement-analysis/index.mjs` 的 `applyAnalysis()`，硬编码只 `replaceSection` 三个固定 section（`Claude 解读 / 歧义点 / 保真差异`），且输入只校验 `claudeInterpretation / ambiguities / fidelityDiff` 三字段（`:143-149,185-193`）。
- 同一套 5 段 renderer 还被「手工创建 / DB 反导出」复用（`project-indexer.ts:662-757`、`export-db-requirements-to-docs.ts`）。

所以全仓已解析需求文档无一例外是 5 段——**改 prompt / 加模板提示无效，必须改生成逻辑**。

### 证实用户洞察：这 3 段是 Console 投影硬契约

用户说「现在的需求文档内容有一些是用来 console ui 的解析对应的」——成立且是硬约束：

`parseRequirementSections()` 按固定标题解析 5 段 → DB 三字段 → API → Web 详情页 + 创建表单（`document-parser.ts:362-380`、`schema.prisma:465-472`、`RequirementDetailPage.tsx`、`App.tsx`）。即 `Claude 解读 / 歧义点 / 保真差异` 既是人读分析、又是 UI 投影锚点，**贸然改 section 结构会断投影链**。这正是本需求的核心张力：**模板写作结构（13 章节） ⚔ Console 投影锚点（3 固定 section）**。

### 归一方向（✅ 已定 · 用户 2026-05-30 拍板）

定为 **C 两层并存**：文档主体按模板 13 章节人读，文末保留 `## Claude 解读 / ## 歧义点 / ## 保真差异` 三锚点供 Console 投影；以「**锚点 = 投影摘要、主体 = 完整文档**」的生成规则约束二者漂移。理由：同时满足「按模板模块」人读诉求 + 投影链零改动，爆炸半径集中在生成端与模板。

**范围经用户拍板扩大**（覆盖 Claude / Codex 初始「列 TODO」建议）：
- ✅ 需求文档：C 两层并存。
- ✅ **其他文档一并纳入本需求**：technical_design / dev_task 的模板对齐同批做（注意：二者成因与机制不同于需求文档——非代码写死、不依赖三锚点，需另行设计各自的模板对齐规范，工作量显著增大）。
- ✅ 存量旧 5 段需求文档：**不强制迁移**，新结构只对新分析生效。

A / B / 第四方案对比见「歧义点」第 1 项。

### slot1_codex 协商要点（job_2deef2dc7289）

核验并补全根因（指出手工 / DB 反导出 renderer 同样输出 5 段，不止 AI apply 路径）；厘清投影「爆炸半径」完整依赖链（SKILL.md 节点契约、reanalyze + schema-ownership-lint、API schema、edit / reindex 复用 parser、Web 详情 / 表单）；推荐 **C** + 旧文档不强迁 + 其他文档列 TODO；`analysis_depth_hint = human-decision`（high confidence）。

### 4 锚点反思

- **同意**：根因是代码写死非规则；3 锚点是投影硬契约；C 在「满足模板诉求 + 不破投影」间平衡最优、爆炸半径最小。
- **保留**：C 的冗余 / 漂移是真实代价，需靠「锚点 = 摘要、主体 = 全文」生成规则约束。
- **盲点**：初查只覆盖 indexer + 表单，漏了 DB 反导出 renderer、reanalyze + lint、edit / reindex、详情页渲染等依赖，经协商补全。
- **决策差异（已记录）**：用户最终选「其他文档一并纳入本轮」，与 Claude / Codex「列 TODO」建议相反——尊重用户决策，范围按此扩大。
- **下一步**：决策已闭合（C + 不迁旧档 + 其他文档纳入）→ 转入 technical_design 节点，分别设计「需求文档 C 方案生成规则」与「technical_design / dev_task 模板对齐」。
## 歧义点

核心歧义 3 项，均已由用户 2026-05-30 拍板闭合：

### 1. 结构归一方向 — ✅ 已闭合：定为 C

| 方案 | 人读符合度 | 投影改动 | 爆炸半径 | 主要代价 |
|---|---|---|---|---|
| A 模板作「Claude 解读」下 ### 子结构 | 低（模块非顶层） | 无 | 最小 | 字面「按模板模块」打折 |
| **✅ C 主体模板 13 章 + 文末 3 锚点（已选）** | 高 | 无（锚点不变） | 小（生成端 / 模板） | 主体 ↔ 锚点信息冗余 / 漂移 |
| B AI 产出 13 章 + parser 多章节聚合字段 | 最高（纯模板） | 大 | 最大（8+ 处 + parser 需双向兼容旧 5 段） | 工程重、回归面广 |
| 第四 独立 structuredAnalysis / sidecar 投影字段 | 最高 | 中（新字段） | 中大（schema / API / 迁移） | 本轮过重，留长期演进 |

**用户拍板 = C**：在「人读纯净度 vs 工程成本」间取平衡，主体满足模板、文末锚点保投影；以「锚点 = 摘要、主体 = 全文」约束漂移。

### 2. 存量旧文档是否迁移 — ✅ 已闭合：不强制迁移

仓内约 10 份完整三锚点 + 1 份部分锚点的旧 5 段文档。**用户拍板 = 不强制迁移**：新结构只对新分析生效，旧文档保持原样、按需手动重排（零回归风险）。

### 3. 「其他文档」边界 — ✅ 已闭合：本轮一并纳入

核验：`technical_design` 节点只校验 `doc_type / requirement_id` + 正文非空，**不解析模板章节**；`dev_task` 走独立物化 / validator，主要投影 frontmatter——二者均非「三锚点 vs 模板」问题，成因是「一直手写、结构自由」。

**用户拍板 = 本轮一起做**（覆盖 Claude / Codex「列 TODO」建议）：technical_design / dev_task 的模板对齐纳入本需求。⚠️ 含义：需在技术设计阶段**额外**设计这两类文档各自的模板对齐规范与落地方式（它们没有需求文档那样的代码写死点，治理机制需另立），整体范围与工作量显著扩大。

**不命中维度**：隐私 / 合规 / 成本 / 外部服务 / 性能 / 安全均不命中——纯本地文档结构与解析，无数据外泄、网络调用、规模或越权问题。
## 保真差异

用户原话 → 澄清后的差异（以原话为准；差异处已获用户 2026-05-30 确认）：

1. 原话「是规则没有限制使用模板还是因为其他问题？」 → 澄清：**是代码写死**——`applyAnalysis()` 硬编码只产出 3 个 section；改 prompt / 规则无效，须改生成逻辑（`lib/requirement-analysis/index.mjs`）。
2. 原话「把……产出的内容和模板做个二次结合……按照模板的模块来产出」 → 澄清并定案：这 3 个 section 同时是 Console 投影锚点，不能简单替换为模板章节；经拍板取 **C 两层并存**（主体模板 13 章 + 文末 3 锚点），非纯文案重排。
3. 原话「其他文档也要思考一下」 → 澄清 + 用户最终决定：经核验 `technical_design` / `dev_task` 与三锚点无关、非同类问题，Claude / Codex 建议列 TODO；**但用户拍板「本轮一起做」**——故 technical_design / dev_task 模板对齐纳入本需求范围。
4. 隐含范围（用户未提存量处理）→ 已闭合：**不强制迁移**已有旧 5 段文档。

无遗留 TBD——归一方向（C）/ 存量迁移（不迁）/ 其他文档边界（本轮纳入）三项均已闭合。
