---
id: lessons-23ee06-project-isolation-audit
title: 多项目隔离审计矩阵 经验沉淀
doc_type: lessons
updated: 2026-06-08
---

# 多项目隔离审计矩阵

> requirement `cmq3m1i8r5ac97ea38323ee06` / short id `23ee06` ｜ 修复后终态审计 ｜ 最后更新: 2026-06-08

## 结论

红项: 0。

矩阵行统计: 绿 29 / 黄 26 / 红 0。黄项均为最终一致投影或 legacy 反查兜底,没有发现当前可触发的跨项目写入红项。

计数口径:
- 前端: 技设原始口径为 246 处 `selectedProjectId` 消费点;当前终态 `rg selectedProjectId web/src` 为 253 处,其中生产文件 225 处、测试/覆盖 28 处。矩阵前端 section 共 26 行,其中生产文件归并 24 行,另把 URL scope 与旧链接基础链路单列点验。
- CcbdClientService: 技设原始口径 15 个构造点;当前终态 `rg "new CcbdClientService" server/src` 为 17 处,其中生产 13 处、测试/fixture 4 处。新增测试构造点也已显式传 scope,一并登记。
- e2e 行按本批归档口径标注为“已编写未实跑(静态归档,实跑留 merge 后预览)”,不作为已验证证据。

## 前端 URL Scope

| 入口/文件归并 | projectId 来源 | 结论 | 证据 |
|---|---|---|---|
| 路由 scope 根: `web/src/App.tsx` | URL 路径段 `/projects/:projectId` | 绿 | `ProjectScopeProvider` 校验并同步 URL 身份: `su-oriel/web/src/App.tsx:143`, `:151`, 路由挂载 `:939-940`; 单测 `web/src/tests/app-redesign.spec.tsx:394`, `:407`, `:416` |
| 旧链接 redirect: requirement/task/document | id 查归属后 redirect 到 scoped URL | 绿 | `LegacyScopedRedirect`: `su-oriel/web/src/App.tsx:171-245`; 单测 `web/src/tests/app-redesign.spec.tsx:425`, `:441` |
| path helper: `web/src/lib/project-paths.ts` | 显式 projectId 参数 / `useProjectScope()` | 绿 | `projectPath` 单点构造: `su-oriel/web/src/lib/project-paths.ts:13`, `:25`, `:32`; sidebar/nav 调用 `web/src/components/layout/Sidebar.tsx:169` |
| `web/src/App.tsx` shell 消费点(22) | URL 同步后的 store 投影 | 绿 | `syncSelectedProjectFromUrl` 是唯一身份写入口: `su-oriel/web/src/App.tsx:97-101`, `:147-152`; silent refresh 不改身份: `:267`, `:309`, `:331`; 测试 `web/src/tests/silent-refresh.spec.ts:105`, `:114` |
| `web/src/stores/project-store.ts` store 投影(12) | URL 同步器写入的 `selectedProjectId` | 黄 | 不再 fallback `projects[0]`: `su-oriel/web/src/stores/project-store.ts:44-49`; `silentRefreshProjects` 保留/清理投影但不切项目: `:89-103`; 黄因仍是异步最终一致 store 投影 |
| `web/src/components/layout/Sidebar.tsx`(4) | App 传入 URL 同步投影 | 绿 | 切项目走 `projectPath` 保留当前 section: `su-oriel/web/src/App.tsx:389-390`, `web/src/components/layout/Sidebar.tsx:169`; e2e 覆盖行静态登记 `e2e/multitab-project-isolation.spec.ts:144` |
| `web/src/pages/requirements/RequirementDetailPage.tsx`(50) | URL 同步投影 | 黄 | API 调用携带 selectedProjectId,如 bind/release: `su-oriel/web/src/pages/requirements/RequirementDetailPage.tsx:862-900`; 黄因页面仍读 store 投影,建议迁 `useProjectScope()` |
| `web/src/pages/slots/SlotsPage.tsx`(24) | URL 同步投影 | 黄 | slots/status/resize/release 均带 projectId: `su-oriel/web/src/pages/slots/SlotsPage.tsx:142-156`, `:188-203`, `:264-277`; server 查询层隔离兜底见后端矩阵 |
| `web/src/components/ai-cli/AiCliSettingsModal.tsx`(13) | URL 同步投影 | 黄 | 项目级设置按 selectedProjectId 读写; 属只读投影消费,建议迁 scope hook |
| `web/src/components/breakdown-review/BreakdownReviewEmbedded.tsx`(12) | URL 同步投影 | 黄 | requirement 内嵌流程消费当前项目; 页面在 scoped route 下最终一致 |
| `web/src/components/main-terminal/MainTerminalLauncher.tsx`(10) | URL 同步投影 | 黄 | main terminal spawn/descriptor 按 projectId 调用; runtime 端点自身再校验 projectId |
| `web/src/pages/settings/SettingsPage.tsx`(8) | URL 同步投影 | 黄 | settings 读写均按当前 projectId; 建议迁 `useProjectScope()` |
| `web/src/pages/tasks/TasksBoardView.tsx`(7) | URL 同步投影 | 黄 | 看板数据来自当前项目 store; 旧链接已 redirect 到 scoped URL |
| `web/src/pages/sprints/SprintsPage.tsx`(7) | URL 同步投影 | 黄 | sprint API 带 projectId; 建议迁 scope hook |
| `web/src/pages/requirements/RequirementsPage.tsx`(7) | URL 同步投影 | 黄 | 列表/刷新状态按 selectedProjectId 调用: `su-oriel/web/src/pages/requirements/RequirementsPage.tsx:21-55` |
| `web/src/pages/ai-cli/AiCliPage.tsx`(7) | URL 同步投影 | 黄 | AI CLI 页面按项目投影加载; runtime 端点需继续保持后端 scope |
| `web/src/components/ai-cli/AiCliPanel.tsx`(7) | URL 同步投影 | 黄 | sidebar 面板消费当前项目; 建议后续收敛为 scope prop |
| `web/src/pages/tasks/TaskDetailPage.tsx`(6) | URL 同步投影 | 黄 | 任务详情在 scoped route 下读当前项目; 旧任务链接 resolver 已按 id 查归属 |
| `web/src/components/slot-requirements-fab/SlotRequirementsFab.tsx`(6) | URL 同步投影 | 黄 | FAB 只读当前 slots 投影; e2e 绑定互不串扰行静态登记 `e2e/multitab-project-isolation.spec.ts:59` |
| `web/src/components/task-detail-v2/AnchorStartStrip.tsx`(5) | URL 同步投影 | 黄 | anchor/start 链路带 projectId; 后端 anchor-terminal 有归属校验 |
| `web/src/pages/reconcile/ReconcileReportsPage.tsx`(4) | URL 同步投影 | 黄 | 项目级 reconcile 读当前 projectId; 建议迁 scope hook |
| `web/src/pages/overview/OverviewPage.tsx`(3) | URL 同步投影 | 黄 | 概览按当前项目加载; 由 ProjectScopeProvider 保证 scoped route |
| `web/src/components/task-detail-v2/sidebar/DerivedFollowupsCard.tsx`(3) | URL 同步投影 | 黄 | followup/派生入口读当前项目; 后端 queue 写入 projectId |
| `web/src/pages/tasks/TaskDetailFullPage.tsx`(2) | URL 同步投影 | 黄 | 全屏任务详情按 URL taskId + 当前 project 投影; 旧链接 redirect 已覆盖 |
| `web/src/pages/runs/RunsPage.tsx`(2) | URL 同步投影 | 黄 | 运行记录按当前项目过滤; EventJournal 另有 `project_id` 过滤 |
| `web/src/pages/documents/DocumentsPage.tsx`(2) | URL 同步投影 | 黄 | 文档详情 URL 驱动,列表按当前项目; 旧文档链接 resolver 已覆盖 |

前端黄项收敛建议:
- 后续把页面/组件内 `useProjectStore((s) => s.selectedProjectId)` 逐步替换为 `useProjectScope()` 或显式 prop,保留 store 仅作兼容投影。
- 加 lint 规则: `selectedProjectId` 只允许 `App.tsx`、`project-store.ts`、少量 layout 适配层直接消费。

## Server Slot / Terminal Endpoints

| 入口 | projectId 来源 | 结论 | 证据 |
|---|---|---|---|
| `GET /api/projects/:projectId/slots` | URL 路径段 | 绿 | `buildSlotProjection(db, projectId)` 查询层过滤: `su-oriel/server/src/modules/slot-binding/slot.routes.ts:133-135`, `:490-523`; 测试 `slot.routes.spec.ts:128`, `:275`, `:340` |
| `GET /api/slots?projectId=` | query 参数 | 绿 | 无 projectId 直接 400: `su-oriel/server/src/modules/slot-binding/slot.routes.ts:138-145` |
| `POST /api/projects/:projectId/slots/resize` | URL 路径段 | 绿 | resize service 以 projectId 调用: `su-oriel/server/src/modules/slot-binding/slot.routes.ts:148-166`; 双项目落点测试 `server-project-isolation.integration.spec.ts:184` |
| `POST /api/projects/:projectId/requirements/:requirementId/bind-slot` | URL 路径段 + requirement 归属校验 | 绿 | requirement `findFirst({id, projectId})`: `su-oriel/server/src/modules/slot-binding/slot.routes.ts:178-201`; 双项目落点测试 `server-project-isolation.integration.spec.ts:184` |
| `POST /api/projects/:projectId/slots/:slotId/release` | URL 路径段 + slot unique `(projectId, slotId)` | 绿 | `projectId_slotId` 查询并 reset 传 projectId: `su-oriel/server/src/modules/slot-binding/slot.routes.ts:228-287`; 测试 `slot.routes.spec.ts:535`, `:582` |
| `POST /api/projects/:projectId/slots/:slotId/renew` | URL 路径段 + slot unique | 绿 | `projectId_slotId` 查询: `su-oriel/server/src/modules/slot-binding/slot.routes.ts:294-339`; 测试 `slot.routes.spec.ts:937` |
| `POST /api/projects/:projectId/slots/:slotId/archive` | URL 路径段 + slot unique | 绿 | payload 写 `project_id`, queue enqueue 写 projectId: `su-oriel/server/src/modules/slot-binding/slot.routes.ts:346-414`; 测试 `slot.routes.spec.ts:983` |
| `POST /api/projects/:projectId/slots/:slotId/cancel-current-job` | URL 路径段 + queue 查询 projectId | 绿 | `findLatestSubmittedQueueRow(db, projectId, slotId)`: `su-oriel/server/src/modules/slot-binding/slot.routes.ts:416-472`, `:692-723`; 测试 `slot.routes.spec.ts:1033` |
| `GET /api/projects/:projectId/requirements/:requirementId/slot-terminal` | URL 路径段 | 绿 | route 传 service target `{projectId, requirementId}`: `su-oriel/server/src/modules/slot-terminal/slot-terminal.routes.ts:25-35`; WS 同 target 校验: `slot-terminal.ws.ts:138-157` |
| `GET /api/projects/:projectId/agent-terminal/:group` | URL 路径段 | 绿 | route 传 service target `{projectId, group}`: `su-oriel/server/src/modules/slot-terminal/slot-terminal.routes.ts:46-56`; WS 同 target 校验: `slot-terminal.ws.ts:168-187` |

## Anchor Terminal Endpoints

| 入口 | projectId 来源 | 结论 | 证据 |
|---|---|---|---|
| `GET /api/anchor-terminal/panes` | query `projectId` + `AnchorAllocation.projectId`; legacy subject 反查兜底 | 黄 | projectId 必填并 `assertAnchorInProject`: `su-oriel/server/src/modules/anchor-terminal/anchor-terminal.routes.ts:50-66`, `:354-392`; 测试 `anchor-terminal.routes.spec.ts:178`, `server-project-isolation.integration.spec.ts:370` |
| `GET /api/anchor-terminal/ws` | query `projectId` + allocation scope | 黄 | attach 前 scope 校验: `su-oriel/server/src/modules/anchor-terminal/anchor-terminal.routes.ts:69-102`; 跨项目拒绝测试 `anchor-terminal.routes.spec.ts:357` |
| `GET /api/anchor-terminal/recordings` | query `projectId`,可选 `anchorId` | 黄 | 无 projectId 400; list 过滤 `filterRecordingsForProject`: `su-oriel/server/src/modules/anchor-terminal/anchor-terminal.routes.ts:238-252`, `:400-408`; 测试 `anchor-terminal.routes.spec.ts:422` |
| `GET /api/anchor-terminal/recordings/:id/cast` | query `projectId` + recording.meta.anchorId 归属校验 | 黄 | cast 读取后 `assertAnchorInProject`: `su-oriel/server/src/modules/anchor-terminal/anchor-terminal.routes.ts:256-267`; 跨项目 403 测试 `anchor-terminal.routes.spec.ts:422` |

anchor-terminal 黄项收敛建议:
- 当前安全状态为 fail-closed,但 legacy allocation `projectId = null` 仍走 subject 反查。建议后续清理/回填所有 legacy anchor allocation,再把 `projectId` 设为新行必填并移除兜底。

## CcbdClientService 构造点

| 入口/构造点 | projectId 来源 | 结论 | 证据 |
|---|---|---|---|
| 类型边界 `CcbdClientServiceOptions` | `projectRoot` / `socketPath` / `anchorSocketResolver` 三选一显式参数 | 绿 | options 必填且构造函数无隐式 root fallback: `su-oriel/server/src/modules/ccbd-client/ccbd-client.service.ts:49`, `:131-140`; 类型测试 `ccbd-client.spec.ts:86` |
| `anchor-dispatch-worker.ts` 2 处 | allocation/subject 解析后的 projectRoot | 绿 | 显式构造: `su-oriel/server/src/modules/anchor-broker/anchor-dispatch-worker.ts:116`, `:275` |
| `user-intent.routes.ts` 2 处 | target projectRoot | 绿 | 显式构造: `su-oriel/server/src/modules/user-intent/user-intent.routes.ts:105`, `:108` |
| `slot.routes.ts` 1 处 | request projectRoot | 绿 | 显式构造: `su-oriel/server/src/modules/slot-binding/slot.routes.ts:811` |
| `slot-context-reset.service.ts` 1 处 | B1 后 clientFactory(projectRoot) | 绿 | 默认工厂显式传 projectRoot: `su-oriel/server/src/modules/slot-binding/slot-context-reset.service.ts:82-84`; 回归测试 `slot-context-reset.service.spec.ts:16` |
| `job-slot-router.ts` 1 处 | Project.localPath | 绿 | route scope 后查询 projectRoot 再构造: `su-oriel/server/src/modules/slot-binding/job-slot-router.ts:373`; 队列测试 `job-slot-router.spec.ts:68`, `:175` |
| `project-onboarding.routes.ts` 2 处 | Project.localPath / socket metadata | 绿 | 显式构造: `su-oriel/server/src/modules/project/project-onboarding.routes.ts:367`, `:417` |
| `requirement-reanalyze.service.ts` 1 处 | planning anchor/project root | 绿 | 显式构造: `su-oriel/server/src/modules/requirement/requirement-reanalyze.service.ts:179` |
| `slot-resize.service.ts` 3 处 | Project.localPath | 绿 | ping/projectView/resize runtime 全显式 projectRoot: `su-oriel/server/src/modules/slot-resize/slot-resize.service.ts:501`, `:512`, `:525`; 测试 `slot-resize.service.spec.ts` |
| `ccbd-client.spec.ts` 2 处 | socketPath / anchorSocketResolver | 绿 | 测试构造点显式: `su-oriel/server/src/modules/ccbd-client/ccbd-client.spec.ts:56`, `:112` |
| `server-project-isolation.integration.spec.ts` 2 处 | fake per-project projectRoot | 绿 | 测试 fixture 显式: `su-oriel/server/src/tests/server-project-isolation.integration.spec.ts:136`, `:140` |

## Queue / EventJournal

| 入口 | projectId 来源 | 结论 | 证据 |
|---|---|---|---|
| AnchorDispatchQueue schema + 修复脚本 | enqueue 上下文直写; legacy 回填 | 绿 | `projectId` 字段与索引: `su-oriel/server/prisma/schema.prisma:278-306`; 回填测试 `anchor-dispatch-queue-project-scope.spec.ts:51`, `:86`, `:104` |
| JobSlotRouter enqueue/tick | enqueue input.projectId / tick 参数 projectId | 绿 | enqueue 新行写 projectId: `su-oriel/server/src/modules/slot-binding/job-slot-router.ts:53-91`; tick `where: { projectId, status }`: `:169-188`; 双项目断言 `server-project-isolation.integration.spec.ts:311` |
| EventJournal list | query `project_id` 可选过滤 | 绿 | schema 支持: `su-oriel/server/src/modules/events/event-journal.schemas.ts:362`; route 查询: `event-journal.routes.ts:44`; 测试 `event-journal-routes.spec.ts:500` |

## 浏览器层覆盖登记

| 入口 | projectId 来源 | 结论 | 证据 |
|---|---|---|---|
| Playwright 双 context 六类用例 | 浏览器 URL `/projects/:projectId/...` | 黄 | 已编写未实跑(按本批归档口径;静态归档,实跑留 merge 后预览),不得作为已验证证据。测试文件: `su-oriel/e2e/multitab-project-isolation.spec.ts:59`, `:93`, `:105`, `:121`, `:136`, `:144` |

## 红黄项处置

红项清单: 无。

黄项清单:
- 前端 21 行: 生产页面/组件仍直接读 store `selectedProjectId`,但该值由 URL 同步器写入,属于最终一致只读投影。建议后续迁移到 `useProjectScope()` 并加 lint 禁止新增直接消费。
- anchor-terminal 4 行: `AnchorAllocation.projectId` 是主路径,但 legacy null 行仍 subject 反查。当前 fail-closed,建议后续回填/清理后移除兜底。
- Playwright 1 行: 用例已落文件,但按本批归档口径不标记为已验证。merge 后预览环境应补跑 `pnpm test:e2e`。

## 验证命令

本切为纯文档审计,未修改生产代码。审计使用:

```bash
rg -n "selectedProjectId" web/src -g '*.ts' -g '*.tsx'
rg -n "new CcbdClientService" server/src -g '*.ts'
rg -n "project_id|anchor-terminal|projectId" server/src/modules server/src/tests
node .../ccb-execute-worktree.mjs guard-docs-ccb --code-root /home/sue/dev/SU-CCB-req-cmq3m1i8r5ac97ea38323ee06 --pretty
```

## 变更记录

| 日期 | 新增 / 变更 |
|------|------------|
| 2026-06-08 | 初版: 23ee06 多项目隔离收口矩阵 |
