---
id: td-attention-notify-52441f
title: 主动消息通知机制 技术设计
doc_type: technical_design
requirement_id: cmpzoupdv863041443e52441f
updated: 2026-06-06
---

# 主动消息通知机制 技术设计

> 一句话：建一个**项目级「该找你的事」单一 derive 源（attention-inbox）**，让 Console 浏览器通知与 tmux sidebar 都只做它的投影；ack/DND 用一张**极小状态表**持久化（承认是小 migration），内容不物化。
>
> **无独立 status** —— 跟随 `requirement_id`（cmpzoupdv…52441f）。需求来源：`docs/02_需求设计/整套oriel和plugin缺少主动的消息通知机制-52441f-需求.md`。

> 经 technical_design 节点产出：双路源码核验（web hook 点 + server schema/通道）+ slot1_codex 设计层协商（job_a8ffa861fee7）+ 用户方向拍板（local-first）。**一处用户拍板待定**：是否接受小 migration（见第十章）。

## 一、设计概述

| 项 | 说明 |
|----|------|
| 名称 | 主动消息通知机制（attention notification） |
| 核心职责 | 当出现「需要你介入 / 你的活已完成」事件时主动触达用户，并支持一键切换进入对应需求 / slot |
| 设计原则 | **单一 attention 源、多端只投影、内容 derive 不物化、仅持久化极小用户状态（ack/DND）、best-effort 非阻塞、正确性优先于时效** |
| 需求来源 | `docs/02_需求设计/整套oriel和plugin缺少主动的消息通知机制-52441f-需求.md` |
| 覆盖范围（本期 A+B） | 项目级 attention derive 源（业务 5 源 + harness agent-waiting 文件源）+ ack/DND 极小持久化 + Console 浏览器通知投影 + tmux sidebar attention 投影 |
| 不覆盖（非目标） | C 硬触达（OS/邮件/IM，仅留 adapter 接口延后）；改 CCB runtime 本体；per-user ack/DND（本地单用户）；attention 内容物化表 |

## 二、方案与架构

```
                 ┌──────────── 单一 attention 源（Console server）────────────┐
 源表(只读) ───► │  attention-inbox.service.computeAttention(projectId)        │
 ReviewIntent    │   批量 derive：ReviewIntent / ConsultRequest / dev_task     │
 ConsultRequest  │   approval+decision / EventJournal(receipt|rejected) /      │
 dev_task doc    │   SlotBinding(unhealthy|recovering) /                        │
 EventJournal    │   ProviderActivity(agent-waiting, activity.json 文件源)      │
 SlotBinding     │   ── left-anti-join AttentionAck(已读) ── 过滤 DND ──►       │
 activity.json   │   AttentionItem[]（含 source-native ref + severity）         │
                 └───────┬───────────────────────────────────┬────────────────┘
                         │ (投影 A)                            │ (投影 B，server 内)
                         ▼                                    ▼
   GET /api/projects/:id/attention (+ack/settings)     computeSlotTipsProjection 扩展
   [P1] /attention/stream (SSE, 并存新通道)             （join 绑定需求的 attention，
                         │                                debounce+content-hash 后写 tips）
                         ▼                                    ▼
   web ConsoleLayout 挂 NotificationManager 单例        managed-config 原子写 [ui.sidebar.view].tips
   poll 5-10s diff → 新高 severity →                    `slot-2: ⚠️待你决策 需求名`
   Notification + 声音 + favicon/title badge            CCB sidebar 自轮询 ~1-2s 显示
   点击 → navigate(/requirements|/tasks) + POST ack
```

| 关键原则 | 说明 |
|----------|------|
| 单一源 | A、B 都调 `attention-inbox.service` 同一 derive，规则只算一遍，绝不在两端各算 |
| 内容不物化 | attention 条目实时 derive（真相留源表），只持久化 ack/DND 极小用户状态 → 避开 projection-freeze 类陈旧 bug |
| 并存通道 | **不改** task-keyed `publishProjectionSignal` / `/api/tasks/:taskId/events`；新增 project attention 通道与之并存 |
| best-effort | 通知 / sidebar 写失败绝不影响主业务；通知必达靠 poll 兜底，不依赖 fail-open hook |
| 时效让位正确性 | poll MVP 5-10s；SSE 作 P1 上限 ~1-2s |

**与现有系统的关系 / 边界**：

| 涉及模块 | 本设计如何动它 | 保留 / 不动什么 |
|----------|----------------|------------------|
| `pending-interactions.service.ts` | 抽取其聚合逻辑到 project 级**批量** derive（新 `attention-inbox.service.ts`），复用映射但改批量查询 | task 级 endpoint 与语义不动 |
| `event-stream.service.ts` | （仅 P1）参照 streamTaskEvents 新增 project attention stream | task-keyed `projectionListeners` / `publishProjectionSignal` / task 订阅者不动 |
| `slot-tips-projection.service.ts` | `computeSlotTipsProjection` join attention，加 ⚠️ 标记；写前加 debounce + content-hash guard | 全量重算 + 原子写 + 项目锁 + 自轮询语义不变 |
| Prisma schema | **新增极小 `AttentionAck` 表 + DND 设置**（小 migration，待用户拍板） | 不新建 attention 内容表；不动 EventJournal / ProjectSettings 现有列语义 |
| CCB runtime activity 工件 | **只读消费** `.ccb/agents/<agent>/provider-runtime/<provider>/activity.json`（经 `ccb.config` slot↔agent 映射定位） | 不改 runtime 本体 / 不改 hook 分类——跨仓发版，且所需信息（`event_name` + `diagnostics.tool_name`）文件已带 |
| web `ui-store.ts` / `ConsoleLayout` | 加 notificationSettings + unread slice；挂 `NotificationManager` 单例 | 现有 toast / 路由 / store 结构不动 |

## 三、关键决策与取舍

- **Fork-1 内容 derive + 极小状态表（选） vs 完整 AttentionItem 内容表（拒）**：选 derive 内容、只持久化 ack/DND。拒内容表——物化 derive 会重蹈 projection-freeze 陈旧坑，MVP 不需要。
- **ack/DND 落点（修正）**：原设想「ack 作 EventJournal 事件 + DND 进 ProjectSettings JSON」——经 Codex 源码核验**不成立**：`EventJournal` subject_type 严格(requirement|subtask)、strict discriminated union、无 `attention_acked`，且是 plugin 主权边界（Console「已读」不该混入）；`ProjectSettings` 仅 3 个 strict JSON 列、payload schema strict。**改为**：新增极小 `AttentionAck(projectId, ref, ackedAt)` 表 + DND 设置（`ProjectSettings.attentionSettingsJson` 新列或 `ProjectAttentionSettings` 表）。**承认这是小 migration / 公共 schema 变更 → 升级用户拍板（第十章）。**
- **Fork-2 poll MVP（选）vs SSE 即做**：选 poll 5-10s 落 MVP（简单、绕开 ADR-0027「SSE push 前端 v1.x 非目标」）；SSE 作 P1，若做则**补 ADR addendum** 并新增并存 project stream，不改坏 task stream。
- **信号通道：并存（选）vs 提升 task 通道（拒）**：拒「把 `publishProjectionSignal` 提到 project 级」——会破坏现有 task 订阅语义；新增并存 project attention 通道。
- **stable ref：source-native（选）vs 粗 subjectType+subjectId+kind（拒）**：用源生 id —— `review_intent:<id>` / `consult_request:<id>` / `event_journal:<eventId>` / `dev_task_approval:<taskKey>/<ref>` / `slot_binding:<slotId>/<state>` / `provider_activity:<agentName>/<providerSessionId>/<reason>`（completed 类追加 idle updated_at 区分批次），避免重启 / 重扫重复弹或误 ack。provider_session_id 非强契约（runtime 仅作 diagnostic identity，mismatch 仍接受）——降级链：provider_session_id → provider_turn_id → ccb_session_id → agentName+provider+reason，末级必配 TTL 防抖。
- **harness 交互层（v1.1 纳入本期）：Console 读 activity.json 特判（选）vs 改 runtime hook 分类（拒）**：CCB runtime（codex-dual，安装产物）已把 agent 交互状态经 7 个 Claude Code hook 落盘 activity.json，且原样携带 `event_name` + `diagnostics.tool_name`；Console derive 侧即可完整识别 权限框（state=pending，PermissionRequest 即时）/ 选项框、计划批准（PreToolUse + AskUserQuestion|ExitPlanMode 特判）/ 等输入（Notification idle 60s→pending），零 migration。改 runtime 分类属跨仓发版且信息无增量——降级为上游可选卫生项（顺带让 tmux sidebar 自身显示更准），不进本需求。
- **拒绝**：改 CCB runtime 本体加通知（runtime 不懂 requirement attention）；启用 CCB 邮件通道做主路径（本机 `lib/mail` 缺失 / 退役）。

## 四、核心流程 / 逻辑

```
computeAttention(projectId):                        # 单一源
  并行批量查 ReviewIntent[pending,projectId] /
    ConsultRequest[pending, taskId∈project 的 task]（ConsultRequest 无 projectId，需 join）/
    Document[kind=dev_task,projectId].approval_records+pending_user_decision /
    EventJournal[projectId, type∈{codex_receipt_ready,codex_rejected,
      state_write_conflict,anchor_dispatch_failed}, 近窗口]（后两类 v1.2 默认进首批，待拍板）/
    SlotBinding[projectId, state∈{unhealthy,recovering}] /
    ProviderActivity[文件源：ccb.config slot↔agent 映射 → 逐 agent 读
      .ccb/agents/<agent>/provider-runtime/<provider>/activity.json：
        state==pending → agent_waiting(reason=permission|input)
        event_name==PreToolUse 且 diagnostics.tool_name∈{AskUserQuestion,ExitPlanMode}
          → agent_waiting(reason=question|plan)
        state==failed 且 slot∈{bound,busy} → agent_failed（failed 在 runtime 有 sticky 语义）
        范围：业务 slot agent（SLOT_IDS）+ main_claude/main_codex（v1.3 用户拍板纳入，
          工件已核验存在）；无绑定模型的 slot（如 slot-4）跳过
        main 完成：state 由 active（持续 ≥60s，derive 侧 in-memory 前态跟踪，与防抖同机制）
          转 idle → agent_completed 一次；<60s 短对话不产生
        抑制：SlotBinding∈{unhealthy,recovering,draining,released} 时显式压掉 agent_waiting
          （让位 slot 健康 attention；stale detector 只读 DB，不会自动让位）
        join SlotBinding 取 requirementId（main/空 → cta 退化为 窗口聚焦 / 项目页；
          sidebar(B) 仍 slot-keyed 不投 main，main 仅 Console(A) 投影）；
        防抖分 reason：permission/question/plan 首次 poll 即报；
          input(idle)/flap 走 10s 防抖；文件缺失/损坏静默跳过]
  → 映射为 AttentionItem{ ref(source-native), kind, severity, subjectType, subjectId, title, cta }
  → left-anti-join AttentionAck[projectId]（按 ref 过滤已读）
  → 过滤 DND（settings.dndUntil）→ 排序（severity desc, time）→ 返回

ack(projectId, ref):  upsert AttentionAck(projectId, ref, ackedAt=now)   # 点击/进入即调

NotificationManager（web，ConsoleLayout 单例）:
  每 5-10s（复用现有 ticker）拉 computeAttention → 与上次 by-ref diff
  新增高 severity item 且非 DND：
    若未授权 → Notification.requestPermission()（一次）
    new Notification(title) + 可选 <audio> 声音 + favicon/title badge
    onclick → navigate(/requirements/:id 或 /tasks/:id?node=) → POST ack
  多标签去重：leader 选举（BroadcastChannel/localStorage）只让一个标签弹

syncSlotTips（扩展，server）:
  attention = computeAttention(projectId)           # 同一源
  rows = activeSlotBindings join requirement
  tips = rows.map(r => hasHighAttention(attention, r.requirementId)
                        ? `${slot}: ⚠️待你决策 ${title}` : `${slot}: ${title}`)
  if contentHash(tips) 变化（debounce 窗口内）→ ensureManagedCcbConfig 原子写   # 防高频狂写

syncSlotTips 触发时机（v1.5 补全——guard 为高频调用而设，触发器必须存在）:
  ① 既有:slot bind/release 链路 + 项目 ccbd 启动/恢复
  ② 新增:server 内项目级周期触发（默认 ~30s，跟随项目 ccbd 生命周期 start/stop，可注入便于测试）
     —— attention 是 derive 源无变化事件，sidebar ⚠️ 的出现与 ack 消失依赖周期重算;
        content-hash guard 保证内容不变零写入，狂写风险已防
  ③ best-effort:plugin hook 水位链路顺带触发（fail-open 不可依赖，仅缩短延迟）
```

## 五、测试策略

- [ ] 单元：`computeAttention` 批量查询无 N+1；ConsultRequest 经 task join 正确纳入
- [ ] 单元：ack 后该 ref 从 derive 结果消失；未 ack 的保留；DND 窗口内全部抑制
- [ ] 单元：source-native ref 稳定——同一源对象跨两次 derive ref 不变（不重复弹）
- [ ] 单元：severity 分级（仅 attention 级进主动弹候选；warning/info 不弹）
- [ ] 单元：provider activity 文件源——state=pending → agent_waiting；PreToolUse + AskUserQuestion/ExitPlanMode 特判；文件缺失 / 损坏静默跳过；同 session 同 reason ref 稳定不重复弹
- [ ] 单元：防抖分 reason——permission/question/plan 首次 poll 即纳入；input(idle)/flap 走 10s 防抖；state=failed 仅 bound/busy slot 纳入；SlotBinding unhealthy/recovering/draining/released 时 agent_waiting 被显式抑制
- [ ] 【实现前阻断 smoke】codex provider：approvals_reviewer=user 下 PermissionRequest 是否触发 activity.json state=pending；不成立 → 启用 fallback（SlotBinding.busy + 已派 job + activity 长时间无进展 → 低一级 agent_attention_suspect 启发式）并回设计修订
- [ ] 单元：main agent 完成——active 持续 ≥60s 转 idle 产生一次 agent_completed；<60s 短对话不产生；ack 后消失；再次 active 后旧 item 不复活
- [ ] 单元：sidebar content-hash guard —— attention 高频变动但 tips 内容不变时**不写**配置
- [ ] 集成：slot-binding 绑定的需求出现 pending review → tips 出现 ⚠️ 标记；ack 后消失
- [ ] 集成：ack endpoint upsert 幂等（重复 ack 同 ref 不报错）
- [ ] 集成：DND 设置读写
- [ ] 组件：NotificationManager 多标签 leader 选举只弹一次；onclick 导航 + ack；权限拒绝时降级为 title/favicon badge
- [ ] 回归：task 级 `/api/tasks/:taskId/events` SSE 与订阅者不受影响

## 六、数据设计

**新增极小用户状态持久化（小 migration，待用户拍板）**：

```prisma
model AttentionAck {            // 仅用户「已读」状态，不物化 attention 内容
  id         String   @id @default(cuid())
  projectId  String
  ref        String   // source-native: review_intent:<id> | consult_request:<id> | event_journal:<eventId> | dev_task_approval:<taskKey>/<ref> | slot_binding:<slotId>/<state>
  ackedAt    DateTime @default(now())
  project    Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  @@unique([projectId, ref])
  @@index([projectId, ackedAt])
}
```

**DND 设置**（二选一，均为小 schema 变更）：① `ProjectSettings` 加 `attentionSettingsJson` 列 + 扩 payload schema；② 新 `ProjectAttentionSettings(projectId, dndUntil, ...)` 表。倾向 ①（贴现有 project 设置）。

**只读源表**（不改）：ReviewIntent / ConsultRequest / Document(dev_task) / EventJournal / SlotBinding / Requirement。**只读文件源**（不改不写）：`.ccb/agents/<agent>/provider-runtime/<provider>/activity.json` + `.ccb/ccb.config`（slot↔agent 映射）。

## 七、接口设计（新增，均 additive）

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/projects/:projectId/attention` | derive 当前 attention 列表（初载 + poll） |
| POST | `/api/projects/:projectId/attention/ack` | `{ ref }` 标记已读（upsert AttentionAck） |
| GET / PUT | `/api/projects/:projectId/attention/settings` | 读写 DND |
| GET | `/api/projects/:projectId/attention/stream` | **（P1）** SSE 并存通道，需补 ADR addendum |

模块落点：新 `server/src/modules/attention-inbox/`（service + projector + routes + types + spec），在 `app.ts` 注册。

## 八、文件结构 / 变更清单

- `[NEW] server/src/modules/attention-inbox/attention-inbox.service.ts`：`computeAttention(projectId)` 批量 derive + ack/DND 过滤
- `[NEW] .../attention-inbox.projector.ts`：源表 → AttentionItem（source-native ref + severity 映射；**attention 域 severity 语义独立**，不照搬 task-event-view timeline 值：EventJournal 首批 4 类（receipt_ready/rejected/state_write_conflict/anchor_dispatch_failed）均为 attention 级——完成类与「系统卡住需介入」硬信号主动弹（需求 §五.1 首批两类 + 本档 §十拍板三硬信号）；slot 健康类 warning 被动；timeline 的 severity 常量表仅作 helper 结构复用，timeline 展示语义不变）
- `[NEW] .../provider-activity.source.ts`：ccb.config slot↔agent 映射 + 读各 agent activity.json → agent_waiting/agent_failed 特判（权限框 / 选项框 / 计划批准 / 等输入 / failed）+ SlotBinding 状态抑制 + ref 降级链
- `[MODIFY] sync/scan 兜底`：journal.jsonl 写入水位 vs DB ingestion 对账，hook fail-open 漏投时触发 reindex（需求「必达兜底」规则的具体机制）
- `[NEW] .../attention-inbox.routes.ts`：list / ack / settings（+ P1 stream）
- `[NEW] .../attention-inbox.types.ts` + `*.spec.ts`
- `[MODIFY] server/prisma/schema.prisma`：`AttentionAck` 表 + DND 设置（小 migration）
- `[MODIFY] slot-tips-projection.service.ts`：join attention + ⚠️ 标记 + debounce/content-hash guard
- `[MODIFY] server/src/app.ts`：注册 attention-inbox 路由
- `[NEW] web/src/components/notifications/NotificationManager.tsx` + `web/src/lib/browser-notify.ts`（Notification/Audio/favicon-title util，多标签 leader）
- `[MODIFY] web/src/stores/ui-store.ts`：notificationSettings + unread slice
- `[MODIFY] web/src/App.tsx`（ConsoleLayout）：挂 `NotificationManager` 单例
- `[DEFER] attention-inbox 内 `AttentionAdapter` 接口占位（C：OS/邮件/IM），本期不实现`

## 九、依赖与配置

| 依赖 | 说明 |
|------|------|
| （无新运行时依赖） | A 用浏览器原生 Notification/Audio/favicon-title；通道用既有 EventSource/fetch；server 复用 Prisma/Fastify；B 复用 managed-config writer |

配置：notificationSettings（browser/sound 开关，浏览器本地）；DND（project 级，server 持久化）。

## 十、迁移影响与风险 + 用户拍板项

**【用户拍板 · 命中 schema/migration 必审门】**

- **是否接受一个小 migration**（新增 `AttentionAck` 极小状态表 + DND 设置列/表）？**已拍板（2026-06-06）：接受**——经核验是正确且最省的持久化路径；localStorage fallback（sidebar 无法共享已读、清缓存即丢）已否决。
- **DND 作用域**：默认 **project 全局**（本地单用户工具，无 User 模型）。
- **交付时效**：默认 **poll MVP（5-10s）先落 → SSE 作 P1**（届时补 ADR-0027 addendum）。
- **首批事件范围（v1.2）**：**已拍板（2026-06-06）：按推荐扩充**——「等待决策 / 完成」两类 + `provider_activity.state=failed` + `state_write_conflict` + `anchor_dispatch_failed`（均已有信号源、频率低、属「系统卡住需介入」硬信号）；`tool_call_denied` 留 P1 观察（高频疲劳风险）。
- **main agents 覆盖（v1.2→v1.3）**：**已拍板（2026-06-06）：纳入**——修正我与 Codex 的「不纳入」默认。用户实操：让 main 跑长任务时人会切去看 slot，main 等输入/干完同样无感知。覆盖 waiting（同 slot 规则）+ 完成（active≥60s→idle 产生一次 agent_completed，短对话不弹）；cta 退化为窗口聚焦/项目页；sidebar(B) 仍 slot-keyed，main 仅 Console(A) 投影。

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| project 级 derive N+1 / 性能 | 中 | 慢 | 批量查询 + 小窗口 + ack 表过滤；规模本地中小 |
| sidebar 高频写配置 | 中 | 抖动 / 狂写 | debounce + content-hash guard，内容不变不写 |
| stable ref 不稳 → 重复弹 / 误 ack | 中 | 体验 | source-native id（见第三章） |
| agent_waiting flap（waiting↔active 快速翻转）重复弹 | 中 | 通知疲劳 | session+reason 级 ref + reason 分级防抖（idle/flap 10s） |
| 幽灵 waiting（agent 异常退出残留 pending 文件） | 低 | 误弹 | derive 显式抑制 unhealthy/recovering/draining/released slot（stale detector 只读 DB，不自动让位）；title 带等待时长 |
| codex 侧 pending 未实测（hook 已装但无 live 样本） | 中 | 漏报最高频「实施 agent 等 approval」 | 实现前阻断 smoke；不成立启用 agent_attention_suspect fallback |
| plugin hook fail-open 漏投（appendEvent notify 被吞，300ms 超时） | 中 | 完成/拒绝类通知不必达 | journal.jsonl 水位对账 + 落后触发 reindex（进首批） |
| Notification→pending 依赖文本 marker 命中 | 低 | 漏报等输入类 waiting | smoke 覆盖三类交互；漏报由 60s idle 重试与业务层信号部分兜底 |
| main 完成通知噪声（短对话频繁 idle） | 中 | 通知疲劳 | active≥60s 阈值 + in-memory 前态跟踪（重启丢失，best-effort 可接受） |
| 浏览器通知 UX 竞态（权限 / 多标签 / 导航失败） | 中 | 重复弹 / 漏弹 | 多标签 leader 选举 + 权限降级 + 测试覆盖 |
| ADR-0027 漂移（SSE） | 低 | 治理质疑 | poll MVP 绕开；SSE 时补 addendum |
| 改坏 task event stream | 低 | 回归 | 并存新通道，不动 task-keyed 通道 + 回归测试 |

## 协商与反思留痕

- **协商（v1.2）**：slot3_codex 1 轮（job_6f06fe2ce572，consult 对抗审）。A1 修正（Notification→pending 依赖文本 marker，「完整覆盖」仅对已知 claude payload 成立）、A2/A3/A4 确认；Q1 codex 侧 PermissionRequest hook 已装 + approvals_reviewer=user 但无 live pending 样本 → 实现前阻断 smoke + fallback；Q2 幽灵 waiting 须 derive 显式抑制（stale detector 只读 DB）；Q3 main agents 不纳入 + 无绑定模型 slot 跳过；Q4 ref 降级链；Q5 防抖分 reason；Q6 failed/state_write_conflict/anchor_dispatch_failed 首批候选 + hook fail-open 须 journal 水位对账兜底。全部采纳。
- **协商（v1.0）**：slot1_codex 设计层 1 轮（job_a8ffa861fee7）。Codex 抓出我设计的实质错误：① Fork-1A「零 migration」不成立（EventJournal/ProjectSettings 均 strict + 主权边界，ack/DND 无法白塞）；② pending-interactions 不能裸 lift（task 级 / N+1，ConsultRequest 无 projectId）；③ 不应提升 task-keyed 信号通道，应并存；④ sidebar 高频写需 debounce/content-hash；⑤ stable ref 必须 source-native。我全部采纳。
- **sc 替代说明**：未单独跑 `/sc:design`、`/sc:analyze`；其视角由**双路源码核验**（web hook 点 + server schema/通道，= /sc:analyze 现有代码影响）+ **我自拟架构草案**（= /sc:design）+ **Codex 设计质疑**覆盖。Codex `analysis_depth_hint=sc-design(high)` 已反映本设计跨 schema/通道的复杂度，相应风险已落第十章。
- **4 锚点反思**：① 同意——ack/DND 须小 migration、ack 不入 EventJournal 主权边界、并存通道、sidebar debounce、source-native ref、批量 derive；② 修正自己——我为躲 schema 必审门做了个站不住的「零 migration」设计，承认小 migration 才是正解；与 Codex 在「内容不物化 + 极小状态」其实收敛同一点，无实质分歧；③ 盲点——把 EventJournal/ProjectSettings 当松 schema、漏 N+1/ConsultRequest 无 projectId、漏 sidebar 高频写、漏 source-native ref、漏浏览器通知竞态测试面；④ 下一步——落本文档 + 升级用户拍板小 migration（带默认）；拍板后再进 task_breakdown，不擅自拆。
- **用户拍板（全部已决，2026-06-06）**：方向 local-first；harness 交互层纳入本期 A+B；小 migration 接受；首批范围 = 两类 + failed/state_write_conflict/anchor_dispatch_failed（tool_call_denied 留 P1）；main agents **纳入**——修正我与 Codex 的默认，用户实操是「main 跑长任务时人去看 slot」，main 完成/等待同样无感知（完成信号 = active≥60s→idle，工件已核验 main_claude/main_codex 均存在）。

## 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-06-04 | v1.0 | 初版：单一 derive 源 + 两投影 + 极小 ack/DND 状态表（承认小 migration）+ 并存 project 通道 + sidebar debounce + source-native ref；poll MVP / SSE P1 |
| 2026-06-06 | v1.1 | 纳入 harness 交互层（用户拍板：本期）：第 6 derive 源 ProviderActivity（activity.json 只读文件源，零 migration），覆盖 权限框/选项框/计划批准/等输入；不改 CCB runtime（跨仓），runtime 分类修正降级为上游可选卫生项 |
| 2026-06-06 | v1.2 | consult round 2（slot3_codex，job_6f06fe2ce572）落档：codex pending 阻断 smoke + fallback、防抖分 reason、SlotBinding 状态显式抑制、ref 降级链、hook fail-open journal 水位对账兜底、state=failed/state_write_conflict/anchor_dispatch_failed 首批候选（待拍板）、main agents 不纳入（待确认） |
| 2026-06-06 | v1.3 | 三项用户拍板落档：migration 接受；首批范围扩三硬信号（tool_call_denied P1）；main agents 纳入（waiting + 完成 active≥60s→idle，cta 退化窗口聚焦，sidebar 仍 slot-keyed）；ref 泛化为 `<agentName>`；main 工件存在性已核验 |
| 2026-06-06 | v1.4 | pr1 review 发现 §八 projector 注记「复用 task-event-view severity 表」与需求 §五.1（完成类主动弹）+ §十拍板（三硬信号需介入）矛盾——timeline 是浏览语义、attention 是触达语义。修正：attention 域 severity 独立，EventJournal 首批 4 类均 attention 级；slot 健康 warning；timeline 不动。随 pr1 review request-changes 执行（review job_4e66c1cc381e 链） |
| 2026-06-06 | v1.5 | pr4 review 发现 §四 漏写 syncSlotTips 触发时机：现状仅 bind/release/启动触发，attention 变化（pending 出现/ack 消失）不会反映到 sidebar，B 舱投影生产不可达；content-hash guard 的存在即暗示高频调用预期但无人声明触发器。补全：周期触发（~30s，项目级，跟随 ccbd 生命周期）+ hook 链路 best-effort。随 pr4 review request-changes 执行（review job_43a92e28824a 链） |
